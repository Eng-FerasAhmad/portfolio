export interface Pages {
    contact: string;
    blog: string;
    techstack: string;
    project: string;
    developer: string;
}

export interface CommonViewModel {
    language: string;
    theme: string;
    pages: Pages;
}
