import styled from 'styled-components';
import { pixelToRem } from '../../utils/utils';
export const ProgressContainer = styled.div`
    width: ${pixelToRem(470)};
    display: flex;
    justify-content: center;
    flex-direction: column;
`;